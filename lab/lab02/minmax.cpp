#include <iostream>
using namespace std;

// Max

// Min
for number in input stream
	int max;
	firstnum = max
		if secondnum is > max
			secondnum = max
		if secondnum < thirdnum
			thirdnum = max
					lastnum -1

int main(){
	// call Max
	// call Min
	return 0;
}